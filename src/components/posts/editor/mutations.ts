//src/components/posts/editor/mutations.ts
//naming convention for files that use mutations hooks

// current logged in user query hook

// mutate the cache directly

// invalidate queries if old data is null
